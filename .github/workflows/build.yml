name: Build Python APK

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout your code
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Set up Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # 3️⃣ Install Ubuntu System Dependencies for Buildozer
      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            git zip unzip openjdk-17-jdk \
            autoconf libtool pkg-config zlib1g-dev \
            libncurses5-dev libncursesw5-dev libtinfo-dev \
            libffi-dev libssl-dev cmake

      # 4️⃣ Install locked pip dependencies
      - name: Install Pip Dependencies
        run: |
          python -m pip install --upgrade pip setuptools
          pip install cython==0.29.33 buildozer virtualenv

      # 5️⃣ Create & Configure buildozer.spec if it doesn't exist
      - name: Create and edit buildozer.spec
        run: |
          if [ ! -f buildozer.spec ]; then
            buildozer init
            # Set permissions and modify defaults
            sed -i 's/# android.permissions = INTERNET/android.permissions = INTERNET, READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE/' buildozer.spec
            sed -i 's/# package.domain = org.test/package.domain = org.example/' buildozer.spec
            sed -i 's/# title = My Application/title = briks/' buildozer.spec
          fi

      # 6️⃣ Build the APK (Accepting Android SDK licenses automatically)
      - name: Build APK with Buildozer
        run: |
          yes | buildozer -v android debug

      # 7️⃣ Upload APK as artifact
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Briks-Python-App
          path: '**/*.apk'
